import 'data/product.dart';

void main(){
  var pruy = Produk();
  pruy.id = 2;
  pruy.name = 'dobleh';
  // pruy._quantity = 1000; // tidak bisa karna access modifier
  // pruy._getQuan(); // tidak bisa karna access modifier
}